% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose.R
\name{diagnose_db}
\alias{diagnose_db}
\title{Diagnose one or several FASTA reference databases}
\usage{
diagnose_db(
  files,
  tax_format = "auto",
  plot = TRUE,
  min_length = 50L,
  check_duplicates = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{files}{(Character vector, required) One or more paths to FASTA files
(plain or gzip).}

\item{tax_format}{(Character, default \code{"auto"}) Taxonomy format to assume
for every file. One of \code{"auto"}, \code{"unite"}, \code{"sintax"},
\code{"greengenes2"}, \code{"pr2"}, or \code{"dada2"}. When \code{"auto"} (default) the
format is detected per file from its headers.}

\item{plot}{(Logical, default \code{TRUE}) Whether to build diagnostic plots.
Requires the \pkg{ggplot2} package; when it is not installed, \verb{$plots}
is \code{NULL} and a message is emitted.}

\item{min_length}{(Integer, default \code{50}) Sequences shorter than this are
counted as "short" and raise a quality warning.}

\item{check_duplicates}{(Logical, default \code{TRUE}) Whether to look for
duplicated sequences. This compares full sequences and can be slow on
very large databases; set to \code{FALSE} to skip.}

\item{verbose}{(Logical, default \code{TRUE}) Whether to print a summary and the
collected warnings via \code{\link[=message]{message()}}.}
}
\value{
An object of class \code{"dbpq_diagnosis"}: a list with components
\describe{
\item{\code{stats}}{A \link[tibble:tibble]{tibble}, one row per file, with
sequence counts, length statistics, and problem counts.}
\item{\code{coverage}}{A long tibble of per-rank annotation coverage
(\code{file}, \code{rank}, \code{n_annotated}, \code{pct_annotated}).}
\item{\code{warnings}}{A tibble of collected issues (\code{file}, \code{check},
\code{severity}, \code{message}); \code{severity} is one of \code{"info"}, \code{"warning"},
\code{"error"}. Cross-file issues have \code{file = NA}.}
\item{\code{cross_file}}{A list describing agreement across files
(\code{formats}, \code{format_agreement}, \code{n_files}).}
\item{\code{plots}}{A list of \pkg{ggplot2} objects (\code{length}, \code{coverage}),
or \code{NULL}.}
}
}
\description{
Runs format, integrity, and quality checks on one or more FASTA reference
databases and returns a structured diagnosis: per-file statistics,
per-rank taxonomic coverage, a table of collected warnings, a cross-file
comparison (which flags problems such as a \strong{mixed taxonomy format} across
files), and (optionally) diagnostic plots.

The three axes checked are:
\describe{
\item{Format}{Is the file valid FASTA? Which taxonomy format is detected
(\code{\link[=detect_tax_format]{detect_tax_format()}})? Are the taxonomy prefixes consistent across
all headers?}
\item{Integrity}{Can the file be read to completion (a truncated gzip
fails here)? Are there empty sequences or duplicated sequence IDs?}
\item{Quality}{Sequence-length distribution and unusually short
sequences, percentage of sequences annotated at each rank, ambiguous
(non-ACGT) bases, duplicated sequences, and unwanted taxonomic values
(\code{\link[=count_unwanted_tax]{count_unwanted_tax()}}).}
}
}
\examples{
unite <- system.file("extdata", "example_unite.fasta", package = "dbpq")
diag <- diagnose_db(unite)
diag$stats

# Several files at once: a mismatched taxonomy format is flagged
sintax <- system.file("extdata", "example_sintax.fasta", package = "dbpq")
diag2 <- diagnose_db(c(unite, sintax))
diag2$warnings
}
\seealso{
\code{\link[=summarize_db]{summarize_db()}}, \code{\link[=detect_tax_format]{detect_tax_format()}}, \code{\link[=count_unwanted_tax]{count_unwanted_tax()}}
}
\author{
Adrien Taudière
}
