% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_midori2_db}
\alias{download_midori2_db}
\title{Download a MIDORI2 reference database}
\usage{
download_midori2_db(
  dest_dir = ".",
  gene = "CO1",
  format = c("dada2", "dada2_species", "SINTAX", "RDP", "BLAST"),
  seq_type = c("UNIQ", "LONGEST"),
  url = NULL,
  verbose = TRUE
)
}
\arguments{
\item{dest_dir}{(Character, default \code{"."}) Directory to save the
downloaded file.}

\item{gene}{(Character, default \code{"CO1"}) Mitochondrial gene marker.
Common values: \code{"CO1"}, \code{"srRNA"} (12S), \code{"lrRNA"} (16S), \code{"Cytb"}.}

\item{format}{(Character, default \code{"dada2"}) One of \code{"dada2"},
\code{"dada2_species"}, \code{"SINTAX"}, \code{"RDP"}, or \code{"BLAST"}.}

\item{seq_type}{(Character, default \code{"UNIQ"}) One of \code{"UNIQ"} (all
unique haplotypes per species) or \code{"LONGEST"} (single longest
sequence per species).}

\item{url}{(Character) Direct download URL. If \code{NULL} (default), the
function provides instructions and the download page URL.}

\item{verbose}{(Logical, default \code{TRUE}) Print progress messages.}
}
\value{
The path to the downloaded file (invisibly), or a message
with download instructions if no URL is provided.
}
\description{
Downloads the MIDORI2 reference database for eukaryotic mitochondrial
genes (COI, 12S, 16S, Cytb, etc.). MIDORI2 provides pre-formatted
FASTA files for multiple classifiers (dada2, SINTAX, RDP, BLAST).
}
\details{
MIDORI2 download URLs include a date-stamped directory path that
changes with each GenBank release, making fully programmatic access
fragile. Visit \url{https://www.reference-midori.info/download.php} to
find the current download URL for your desired gene and format, then
pass it via the \code{url} parameter.

Files are typically named following this pattern:
\verb{MIDORI2_\{TYPE\}_NUC_SP_GB\{VERSION\}_\{GENE\}_\{FORMAT\}.fasta.gz}

Please cite: Leray M et al. (2022) MIDORI2: A collection of quality
controlled, preformatted, and regularly updated reference databases
for taxonomic assignment of eukaryotic mitochondrial sequences.
Environmental DNA 4:894-907. \doi{10.1002/edn3.303}
}
\examples{
\dontrun{
# Get instructions for downloading MIDORI2
download_midori2_db()

# Download with a specific URL
download_midori2_db(
  dest_dir = "databases",
  url = "https://reference-midori.info/download/Databases/..."
)
}
}
\seealso{
\code{\link[=format2sintax]{format2sintax()}}, \code{\link[=format2dada2]{format2dada2()}}
}
\author{
Adrien Taudière
}
