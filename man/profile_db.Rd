% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{profile_db}
\alias{profile_db}
\title{Profile the taxonomic content of one or several FASTA databases}
\usage{
profile_db(
  files,
  tax_format = "auto",
  weight_by_seqs = FALSE,
  plot = TRUE,
  venn_max = 7L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{files}{(Character vector, required) One or more paths to FASTA files
(plain or gzip).}

\item{tax_format}{(Character, default \code{"auto"}) Taxonomy format, passed to
\code{\link[=diagnose_db]{diagnose_db()}} and used for taxon extraction. One of \code{"auto"}, \code{"unite"},
\code{"sintax"}, \code{"greengenes2"}, \code{"pr2"}, or \code{"dada2"}.}

\item{weight_by_seqs}{(Logical, default \code{FALSE}) When \code{TRUE}, the
cross-database UpSet plot weights each intersection by the number of
sequences (summed over the databases containing each taxon) rather than
by the number of taxa. Forces an UpSet plot (Venn diagrams cannot be
weighted).}

\item{plot}{(Logical, default \code{TRUE}) Whether to build plots. Requires
\pkg{ggplot2}; the comparison plots additionally require
\pkg{ggVennDiagram} and/or \pkg{ComplexUpset}.}

\item{venn_max}{(Integer, default \code{7}) Maximum number of databases for which
a Venn diagram is drawn; beyond this an UpSet plot is used. (Also forced to
UpSet when \code{weight_by_seqs = TRUE}.)}

\item{verbose}{(Logical, default \code{TRUE}) Whether to show a \pkg{cli}
progress bar while reading files and print a summary report.}

\item{...}{Further arguments passed to \code{\link[=diagnose_db]{diagnose_db()}} (e.g. \code{min_length},
\code{check_duplicates}).}
}
\value{
An object of class \code{"dbpq_profile"}: a list with components
\describe{
\item{\code{diagnosis}}{The \code{\link[=diagnose_db]{diagnose_db()}} result (a \code{dbpq_diagnosis}).}
\item{\code{taxa}}{A long \link[tibble:tibble]{tibble} (\code{file}, \code{rank}, \code{taxon},
\code{n_seqs}) of every taxon found at every rank.}
\item{\code{richness}}{A tibble (\code{file}, \code{rank}, \code{n_levels},
\code{n_seqs_annotated}) of per-rank taxonomic richness.}
\item{\code{comparison}}{\code{NULL} for a single file; otherwise a list with
\code{per_db} and \code{signatures} tibbles, per-rank \code{membership} data frames,
and per-rank \code{plots}.}
\item{\code{plots}}{A list with \code{richness} (a ggplot) and \code{comparison} (a
named list of per-rank Venn/UpSet plots, or \code{NULL}).}
}
}
\description{
Builds a taxonomic profile of one or more FASTA reference databases. It
first runs \code{\link[=diagnose_db]{diagnose_db()}} (format/integrity/quality checks) and then adds:
\itemize{
\item a \strong{richness} table and bar plot giving the number of distinct taxa
(levels) annotated at each taxonomic rank, and
\item when several databases are supplied, a \strong{cross-database comparison} of
the taxa present at each rank, drawn as a Venn diagram
(\pkg{ggVennDiagram}, up to \code{venn_max} databases) or an UpSet plot
(\pkg{ComplexUpset}), with one plot per rank.
}

By default the comparison overlaps the \emph{sets of distinct taxon names} at
each rank (presence/absence). With \code{weight_by_seqs = TRUE} the UpSet bars
instead show the total number of sequences backing the taxa in each
intersection (summed across the databases that contain each taxon).
}
\examples{
unite <- system.file("extdata", "example_unite.fasta", package = "dbpq")
prof <- profile_db(unite, verbose = FALSE)
prof$richness

# Compare two databases (needs ggVennDiagram / ComplexUpset for the plots)
\donttest{
sintax <- system.file("extdata", "example_sintax.fasta", package = "dbpq")
prof2 <- profile_db(c(unite, sintax), verbose = FALSE)
prof2$comparison$signatures
}
}
\seealso{
\code{\link[=diagnose_db]{diagnose_db()}}, \code{\link[=summarize_db]{summarize_db()}}, \code{\link[=list_ranks_db]{list_ranks_db()}}
}
\author{
Adrien Taudière
}
