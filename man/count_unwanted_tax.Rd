% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{count_unwanted_tax}
\alias{count_unwanted_tax}
\title{Count unwanted values in a taxonomy table}
\usage{
count_unwanted_tax(
  x,
  patterns = unwanted_tax_patterns_default(),
  tax_format = "auto"
)
}
\arguments{
\item{x}{Either:
\itemize{
\item a character string giving the path to a FASTA file (plain or gzip), or
\item a \link[phyloseq:phyloseq-class]{phyloseq} object with a taxonomy table.
}}

\item{patterns}{(Character vector) Regular expressions to search for.
When MiscMetabar is installed, defaults to
\link[MiscMetabar:unwanted_tax_patterns]{MiscMetabar::unwanted_tax_patterns}; otherwise falls back to a
built-in copy of the same patterns. See \strong{Details}.}

\item{tax_format}{(Character) Taxonomy format of the FASTA file. One of
\code{"unite"}, \code{"sintax"}, \code{"greengenes2"}, \code{"pr2"}, or \code{"auto"}.
Only used when \code{x} is a file path. If \code{"auto"} (default), the format
is detected with \code{\link[=detect_tax_format]{detect_tax_format()}}. Ignored when \code{x} is a phyloseq
object.}
}
\value{
A \link[tibble:tibble]{tibble} with columns:
\describe{
\item{\code{pattern}}{The regular expression that matched.}
\item{\code{description}}{A human-readable label for the pattern.}
\item{\code{rank}}{The taxonomic rank (column name) where matches were
found.}
\item{\code{n_matches}}{Number of values matching the pattern in that
rank.}
\item{\code{example_values}}{Up to 3 unique matching values
(comma-separated).}
}
Rows with zero matches are omitted.
}
\description{
Scans a taxonomy table for common problematic values such as NA-like
strings, placeholder labels (\code{"unclassified"}, \code{"unknown"}, etc.),
and empty QIIME-style rank prefixes. The input can be a
\link[phyloseq:phyloseq-class]{phyloseq} object or a FASTA reference database
file.

Returns a tibble summarising, for each pattern found, how many matches
occur in each taxonomic rank.
}
\details{
The default patterns are:
\describe{
\item{\code{"^[Nn][Aa][Nn]?$"}}{NaN, nan, NA, na}
\item{\code{"^[Nn]/[Aa]$"}}{N/A, n/a}
\item{\code{"^[Nn]one$"}}{None, none}
\item{\code{"^$"}}{empty string}
\item{\code{"^\\\\s+$"}}{whitespace only}
\item{\code{"[Uu]nclassified"}}{unclassified, Unclassified, xxx_unclassified}
\item{\code{"[Uu]nknown"}}{unknown, Unknown, xxx_unknown}
\item{\code{"[Uu]nidentified"}}{unidentified, Unidentified}
\item{\code{"[Uu]ncultured"}}{uncultured, Uncultured}
\item{\code{"[Ii]ncertae[_\\\\s]?[Ss]edis"}}{incertae_sedis, Incertae sedis}
\item{\code{"^[Mm]etagenome$"}}{metagenome, Metagenome}
\item{\code{"^[Ee]nvironmental"}}{environmental, Environmental}
\item{\code{"^[kpcofgs]__$"}}{empty QIIME-style rank prefixes}
}
}
\examples{
# From a FASTA file (no unwanted values in example data)
db <- system.file("extdata", "example_unite.fasta", package = "dbpq")
count_unwanted_tax(db)

if(requireNamespace("MiscMetabar", quietly = TRUE)) {
 ref_fasta <- Biostrings::readDNAStringSet(system.file("extdata",
   "mini_UNITE_fungi.fasta.gz",
   package = "MiscMetabar", mustWork = TRUE
 ))
data("data_fungi_mini", package = "MiscMetabar")
physeq_new <- MiscMetabar::assign_mmseqs2(
   MiscMetabar::data_fungi_mini,
   ref_fasta = ref_fasta,
   behavior = "add_to_phyloseq"
 )
count_unwanted_tax(physeq_new)
}
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>

}
\seealso{
\code{\link[=summarize_db]{summarize_db()}}, \code{\link[=list_ranks_db]{list_ranks_db()}}
}
\author{
Adrien Taudière
}
