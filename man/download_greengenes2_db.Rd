% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_greengenes2_db}
\alias{download_greengenes2_db}
\title{Download a Greengenes2 reference database}
\usage{
download_greengenes2_db(
  dest_dir = ".",
  version = "2024.09",
  format = c("dada2", "dada2_species", "fasta"),
  tax_format = c("dada2", "sintax", "keep"),
  verbose = TRUE
)
}
\arguments{
\item{dest_dir}{(Character, default \code{"."}) Directory to save the
downloaded file.}

\item{version}{(Character, default \code{"2024.09"}) Greengenes2 version
in \code{YYYY.MM} format.}

\item{format}{(Character, default \code{"dada2"}) One of:
\itemize{
\item \code{"dada2"}: dada2-formatted training set from Zenodo (recommended
for \code{dada2::assignTaxonomy()}).
\item \code{"dada2_species"}: species-level training set for
\code{dada2::assignTaxonomy()} (includes species).
\item \code{"fasta"}: plain FASTA sequences from the FTP server.
}}

\item{tax_format}{(Character, default \code{"dada2"}) How to write taxonomy in
the headers of the \code{"dada2"}/\code{"dada2_species"} training set. The
Greengenes2 trainset ships with \code{d__}/\code{p__} rank prefixes, which
\code{dada2::assignTaxonomy()} and \code{MiscMetabar::add_new_taxonomy_pq()}
reject. One of:
\itemize{
\item \code{"dada2"}: strip the prefixes to unprefixed, positional dada2
(\verb{>Bacteria;Pseudomonadota;...;}).
\item \code{"sintax"}: rewrite as \verb{>ID;tax=d:Bacteria,p:...}.
\item \code{"keep"}: leave the original \code{d__}-prefixed headers untouched.
Ignored for \code{format = "fasta"}.
}}

\item{verbose}{(Logical, default \code{TRUE}) Print progress messages.}
}
\value{
The path to the downloaded file (invisibly).
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Downloads the Greengenes2 16S rRNA database. By default, downloads the
dada2-formatted training sets from Zenodo (maintained by Benjamin
Callahan). Alternatively, downloads backbone sequences from the
Greengenes2 FTP server.

Note that Greengenes2 uses \code{d__} (domain) instead of \code{k__} (kingdom)
as the first rank prefix. Use \code{tax_format = "greengenes2"} with
\code{\link[=summarize_db]{summarize_db()}} and \code{\link[=list_ranks_db]{list_ranks_db()}} for correct parsing.
}
\details{
The dada2-formatted files are maintained by Benjamin Callahan on Zenodo
and are the same source as the SILVA dada2 training sets. See
\url{https://benjjneb.github.io/dada2/training.html} for details.

The Greengenes2 trainset uses \code{d__}/\code{p__} rank prefixes. By default
(\code{tax_format = "dada2"}) the prefixes are stripped so the file is directly
usable by \code{dada2::assignTaxonomy()} and \code{add_new_taxonomy_pq()}.

Please cite: McDonald D et al. (2024) Greengenes2 unifies microbial
data in a single reference tree. Nature Biotechnology 42:715-718.
\doi{10.1038/s41587-023-01845-1}
}
\examples{
\dontrun{
# Download dada2-formatted Greengenes2
download_greengenes2_db(dest_dir = "databases")

# Download plain FASTA from FTP
download_greengenes2_db(dest_dir = "databases", format = "fasta")
}
}
\seealso{
\code{\link[=format2dada2]{format2dada2()}}, \code{\link[=tax_prefixes]{tax_prefixes()}}
}
\author{
Adrien Taudière
}
