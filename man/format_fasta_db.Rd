% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{format_fasta_db}
\alias{format_fasta_db}
\title{Convert a FASTA database to a specified taxonomy format}
\usage{
format_fasta_db(
  fasta_db = NULL,
  taxnames = NULL,
  output_format = c("sintax", "unite", "greengenes2", "dada2", "dada2_species"),
  input_format = "auto",
  output_path = NULL
)
}
\arguments{
\item{fasta_db}{(Character) Path to a FASTA file (plain or gzipped).
Mutually exclusive with \code{taxnames}.}

\item{taxnames}{(Character vector) Taxonomy header strings (without
leading \code{>}). Mutually exclusive with \code{fasta_db}.}

\item{output_format}{(Character) Target format. One of \code{"sintax"},
\code{"unite"}, \code{"greengenes2"}, \code{"dada2"}, \code{"dada2_species"}.}

\item{input_format}{(Character, default \code{"auto"}) Input format. One of
\code{"auto"} (auto-detect via \code{\link[=detect_tax_format]{detect_tax_format()}}), \code{"sintax"},
\code{"unite"}, \code{"greengenes2"}.}

\item{output_path}{(Character) If provided and \code{fasta_db} is used, write
the reformatted FASTA to this path and return the \code{DNAStringSet}
invisibly.}
}
\value{
If \code{taxnames} is used, a character vector of reformatted headers.
If \code{fasta_db} is used, a \code{DNAStringSet} with reformatted names
(invisibly when \code{output_path} is given).
}
\description{
Detects (or uses) the input taxonomy format and rewrites sequence headers
to the requested output format. This is the primary conversion function;
\code{\link[=format2sintax]{format2sintax()}}, \code{\link[=format2dada2]{format2dada2()}}, and \code{\link[=format2dada2_species]{format2dada2_species()}} are
convenience wrappers around it.

Supported \strong{input} formats (prefix-based, with detectable rank labels):
\code{"sintax"}, \code{"unite"}, \code{"greengenes2"}.

Supported \strong{output} formats:
\itemize{
\item \code{"sintax"} — VSEARCH/USEARCH SINTAX (\verb{>ID;tax=k:Kingdom,p:Phylum,...})
\item \code{"unite"} — UNITE default (\verb{>ID;k__Kingdom;p__Phylum;...})
\item \code{"greengenes2"} — Greengenes2 (\verb{>ID d__Domain;p__Phylum;...})
\item \code{"dada2"} — Unprefixed semicolon-delimited (\verb{>Kingdom;Phylum;...;})
\item \code{"dada2_species"} — For \code{dada2::addSpecies()} (\verb{>ID Genus Species})
}

Positional formats (\code{"pr2"}, \code{"dada2"}) can be detected by
\code{\link[=detect_tax_format]{detect_tax_format()}} but cannot be used as input for conversion because
they lack rank labels.
}
\examples{
# UNITE → SINTAX
format_fasta_db(
  taxnames = "AB123;k__Fungi;p__Ascomycota;c__Sordariomycetes",
  output_format = "sintax"
)

# SINTAX → UNITE
format_fasta_db(
  taxnames = "AB123;tax=k:Fungi,p:Ascomycota,c:Sordariomycetes",
  output_format = "unite"
)

# Greengenes2 → dada2
format_fasta_db(
  taxnames = "abc123 d__Bacteria;p__Pseudomonadota;g__Escherichia",
  output_format = "dada2"
)
}
\seealso{
\code{\link[=detect_tax_format]{detect_tax_format()}}, \code{\link[=format2sintax]{format2sintax()}}, \code{\link[=format2dada2]{format2dada2()}},
\code{\link[=format2dada2_species]{format2dada2_species()}}
}
\author{
Adrien Taudière
}
