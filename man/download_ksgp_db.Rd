% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_ksgp_db}
\alias{download_ksgp_db}
\title{Download the KSGP or GTDB+ reference database}
\usage{
download_ksgp_db(
  dest_dir = ".",
  database = c("KSGP", "GTDB_plus", "GTDB_cleaned"),
  file_type = c("fasta", "tax", "archive"),
  annotation = c("sintax", "lca", "ksgp_plus"),
  tax_format = c("dada2", "sintax", "none"),
  version = "3.1",
  verbose = TRUE
)
}
\arguments{
\item{dest_dir}{(Character, default \code{"."}) Directory to save the
downloaded file.}

\item{database}{(Character, default \code{"KSGP"}) One of:
\itemize{
\item \code{"KSGP"}: Full KSGP SSU database (Archaea + Bacteria + Eukaryota).
\item \code{"GTDB_plus"}: Cleaned GTDB 16S sequences with PR2 and MIDORI2.
\item \code{"GTDB_cleaned"}: Cleaned GTDB 16S sequences only (no eukaryote
supplement).
}}

\item{file_type}{(Character, default \code{"fasta"}) One of:
\itemize{
\item \code{"fasta"}: FASTA file with SSU sequences.
\item \code{"tax"}: Taxonomy file (\code{.tax}) with taxonomic annotations.
\item \code{"archive"}: Complete \code{.tar.gz} archive (all KSGP files, all
annotation variants). Only available for \code{database = "KSGP"}.
}}

\item{annotation}{(Character, default \code{"sintax"}) Taxonomic annotation
method. One of \code{"sintax"}, \code{"lca"}, or \code{"ksgp_plus"}. Only applies
when \code{database = "KSGP"} and \code{file_type = "tax"}. Only \code{"sintax"} is
available for version \code{"1.0"}.}

\item{tax_format}{(Character, default \code{"dada2"}) When \code{file_type = "fasta"},
also download the companion \code{.tax} file and merge its taxonomy into the
FASTA headers (matched by sequence ID), so the file feeds
\code{MiscMetabar::add_new_taxonomy_pq()}. One of \code{"dada2"}, \code{"sintax"}, or
\code{"none"} (keep accession-only headers). Sequences whose ID is absent from
the \code{.tax} (e.g. the SILVA-derived portion) keep accession-only headers.
Ignored for \code{file_type = "tax"} / \code{"archive"}.}

\item{version}{(Character, default \code{"3.1"}) KSGP version. Known versions:
\code{"3.1"} (2025, recommended) and \code{"1.0"}.}

\item{verbose}{(Logical, default \code{TRUE}) Print progress messages.}
}
\value{
The path to the downloaded file (invisibly).
}
\description{
Downloads the KSGP (Karst, Silva, GTDB, and PR2) reference database for
SSU rRNA taxonomic assignment, particularly optimized for Archaea
communities. KSGP combines near full-length rRNA sequences from Karst
et al. 2018, re-annotated SILVA prokaryote SSU sequences, cleaned GTDB 16S
sequences, PR2 eukaryote 18S sequences, and MIDORI2 mitochondrial
sequences. Taxonomy is based on GTDB, providing phylogenetically consistent
classification.

Also provides access to the GTDB+ and GTDB_cleaned databases built as
intermediate steps during KSGP construction.

Three annotation variants are available for \code{database = "KSGP"} and
\code{file_type = "tax"}:
\itemize{
\item \code{"sintax"} (default): SINTAX-based taxonomic assignments (KSGP Sintax
in the paper). Available for all versions.
\item \code{"lca"}: Conservative lowest common ancestor assignments (KSGP LCA).
Only available for version \code{"3.1"}.
\item \code{"ksgp_plus"}: Similarity-clustered putative taxa (KSGP+). Only
available for version \code{"3.1"}.
}
}
\details{
The KSGP FASTA and taxonomy files are separate downloads. To use KSGP
for taxonomic assignment:
\itemize{
\item With VSEARCH SINTAX: download both the FASTA (\code{file_type = "fasta"})
and the SINTAX taxonomy (\code{file_type = "tax"}, \code{annotation = "sintax"}).
\item With LotuS2: the KSGP database is integrated directly.
\item For a complete set of all files: use \code{file_type = "archive"}.
}

KSGP substantially improves Archaea annotation over SILVA and Greengenes2:
Class and Order assignments increase by 2.7x and 4.2x respectively.

Please cite: Grant A et al. (2025) KSGP 3.1: improved taxonomic annotation
of Archaea communities using LotuS2, the genome taxonomy database and
RNAseq data. ISME Communications 5(1): ycaf094.
\doi{10.1093/ismeco/ycaf094}
}
\examples{
\dontrun{
# Download KSGP v3.1 FASTA
download_ksgp_db(dest_dir = "databases")

# Download KSGP v3.1 LCA taxonomy file
download_ksgp_db(
  dest_dir = "databases",
  file_type = "tax",
  annotation = "lca"
)

# Download KSGP+ taxonomy file
download_ksgp_db(
  dest_dir = "databases",
  file_type = "tax",
  annotation = "ksgp_plus"
)

# Download the complete KSGP archive (all annotation variants)
download_ksgp_db(dest_dir = "databases", file_type = "archive")

# Download GTDB+ (cleaned GTDB + PR2 + MIDORI2)
download_ksgp_db(dest_dir = "databases", database = "GTDB_plus")
}
}
\seealso{
\code{\link[=download_silva_db]{download_silva_db()}}, \code{\link[=download_pr2_db]{download_pr2_db()}}, \code{\link[=format2sintax]{format2sintax()}}
}
\author{
Adrien Taudière
}
