% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{add_sh_to_taxonomy}
\alias{add_sh_to_taxonomy}
\title{Annotate query sequences with UNITE Species Hypothesis (SH) names}
\usage{
add_sh_to_taxonomy(
  query_fasta,
  unite_db,
  vsearchpath = find_vsearch(),
  id = 0.97,
  maxaccepts = 1,
  maxrejects = 32,
  nproc = 1,
  top_hits_only = TRUE,
  keep_temporary_files = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{query_fasta}{(Character) Path to a FASTA file containing the
query sequences (e.g. ASVs or OTUs).}

\item{unite_db}{(Character) Path to a UNITE reference FASTA file.
The file can be in any format (SINTAX, UNITE default, etc.) as long
as the sequence identifiers contain SH names as the first
\code{|}-delimited field. Files downloaded via \code{\link[=download_unite_db]{download_unite_db()}} meet
this requirement.}

\item{vsearchpath}{(Character) Path to the vsearch executable.
Defaults to \code{\link[MiscMetabar:find_vsearch]{MiscMetabar::find_vsearch()}}.}

\item{id}{(Numeric, default \code{0.97}) Minimum sequence identity
threshold (0–1) for vsearch \code{--id} parameter.}

\item{maxaccepts}{(Integer, default \code{1}) Maximum number of hits to
accept per query. Set to \code{0} for unlimited hits (useful when
multiple equally-good matches may have different SH names).}

\item{maxrejects}{(Integer, default \code{32}) Maximum number of
rejected hits before stopping the search for a query.}

\item{nproc}{(Integer, default \code{1}) Number of threads for vsearch.}

\item{top_hits_only}{(Logical, default \code{TRUE}) If \code{TRUE}, only
report the top hit per query (highest identity). If \code{FALSE},
report all accepted hits, which is useful for detecting
ambiguous SH assignments.}

\item{keep_temporary_files}{(Logical, default \code{FALSE}) If \code{TRUE},
do not delete the temporary blast6 output file after parsing.}

\item{verbose}{(Logical, default \code{FALSE}) Print vsearch progress
messages.}
}
\value{
A data.frame with columns:
\itemize{
\item \code{query}: Query sequence identifier.
\item \code{sh_name}: Species Hypothesis name extracted from the
best database hit (e.g. \code{SH123456.09FU}), or \code{NA} if no
hit was found.
\item \code{target}: Full identifier of the matched database
sequence.
\item \code{pct_id}: Percent identity of the match.
\item \code{aln_len}: Alignment length.
\item \code{mismatches}: Number of mismatches.
\item \code{e_value}: E-value of the match.
\item \code{is_ambiguous}: Logical; \code{TRUE} if multiple hits with
the same top identity disagree on the SH name.
}
}
\description{
Runs \code{vsearch --usearch_global} to match query sequences against a
UNITE reference database, then extracts the Species Hypothesis (SH)
identifier from each best hit. The SH name is the first \code{|}-delimited
field in UNITE sequence identifiers (e.g. \code{SH123456.09FU}).

This function ports the logic of the nf-core/ampliseq
\code{add_sh_to_taxonomy.py} script into R, using vsearch directly
instead of requiring external lookup tables.
}
\examples{
\dontrun{
# Download a UNITE database first
unite_file <- download_unite_db(
  version = "10.0",
  tax_format = "sintax",
  kingdom = "fungi"
)

# Annotate ASVs with SH names
sh_res <- add_sh_to_taxonomy(
  query_fasta = "asvs.fasta",
  unite_db = unite_file,
  id = 0.97
)
head(sh_res)

# Check for ambiguous assignments
sh_res[sh_res$is_ambiguous, ]
}
}
\seealso{
\code{\link[=download_unite_db]{download_unite_db()}}, \code{\link[MiscMetabar:is_vsearch_installed]{MiscMetabar::is_vsearch_installed()}},
\code{\link[MiscMetabar:find_vsearch]{MiscMetabar::find_vsearch()}}
}
\author{
Adrien Taudière
}
