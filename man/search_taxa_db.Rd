% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{search_taxa_db}
\alias{search_taxa_db}
\title{Search sequences by taxonomic name in a FASTA database}
\usage{
search_taxa_db(
  file,
  taxa,
  match = c("all", "any"),
  case_sensitive = FALSE,
  output_path = NULL
)
}
\arguments{
\item{file}{(Character, required) Path to a FASTA file (plain or gzip).}

\item{taxa}{(Character, required) One or more taxonomic name parts to
search for in sequence headers, e.g. \code{c("Apiotrichum", "akiyoshidainum")}.}

\item{match}{(Character, default \code{"all"}) Whether a header must contain
all (\code{"all"}) or at least one (\code{"any"}) of the \code{taxa} parts.}

\item{case_sensitive}{(Logical, default \code{FALSE}) If \code{TRUE}, matching is
case-sensitive.}

\item{output_path}{(Character, default \code{NULL}) Optional path to write the
matched sequences to a FASTA file. A \code{.gz} extension produces a gzipped
file. When set, the \link[Biostrings:DNAStringSet-class]{Biostrings::DNAStringSet} is returned
invisibly.}
}
\value{
A \link[Biostrings:DNAStringSet-class]{Biostrings::DNAStringSet} of the matching sequences,
with original headers preserved (including all taxonomy information).
An empty \code{DNAStringSet} (length 0) is returned when nothing matches.
}
\description{
\if{html}{\out{
<a href="https://adrientaudiere.github.io/MiscMetabar/articles/Rules.html#lifecycle">
<img src="https://img.shields.io/badge/lifecycle-experimental-orange" alt="lifecycle-experimental"></a>
}}


Searches a FASTA database (plain or gzip) for sequences whose header
matches a set of taxonomic name parts. This is useful to retrieve all
sequences assigned to a given taxon across databases that use different
header formats (e.g. UNITE \verb{g__Apiotrichum;s__akiyoshidainum} vs SINTAX
\verb{k:Fungi,...,g:Apiotrichum,s:akiyoshidainum}).

Matching is case-insensitive by default and based on substrings, so the
rank prefixes and separators used by a given format do not matter. Provide
multiple name parts in \code{taxa} to narrow the search: by default a header
matches only when \strong{all} parts are present (\code{match = "all"}); use
\code{match = "any"} to match when at least one part is present.

On Linux/macOS, filtering is done with \code{awk} in a single streaming pass
over the file, so memory usage stays low even for multi-GB reference
databases — only the matched records are loaded into R. On Windows (or
whenever \code{awk} is unavailable), the function automatically falls back to
an in-memory \code{\link[Biostrings:readDNAStringSet]{Biostrings::readDNAStringSet()}} filter, which is slower and
more memory-hungry for large databases but produces the same result.
}
\examples{
db <- system.file("extdata", "example_unite.fasta", package = "dbpq")

# Retrieve all Amanita sequences (single name part)
search_taxa_db(db, "Amanita")

# Narrow to a species with two name parts (AND logic)
search_taxa_db(db, c("Amanita", "muscaria"))

# Match any of several genera (OR logic)
search_taxa_db(db, c("Amanita", "Fusarium"), match = "any")

# Write the result to a gzipped FASTA file and read it back
out <- tempfile(fileext = ".fasta.gz")
search_taxa_db(db, "Boletus", output_path = out)
Biostrings::readDNAStringSet(out)
}
\seealso{
\code{\link[=filter_db]{filter_db()}} for a shell-based header filter that writes to a
file; \code{\link[=format_fasta_db]{format_fasta_db()}} to reformat the retrieved sequences.
}
\author{
Adrien Taudière
}
