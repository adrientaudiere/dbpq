% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modify.R
\name{cutadapt_rm_primers_db}
\alias{cutadapt_rm_primers_db}
\title{Remove primers from a FASTA database using cutadapt}
\usage{
cutadapt_rm_primers_db(
  ref_fasta,
  output = NULL,
  primer_fw = NULL,
  primer_rev = NULL,
  discard_untrimmed = TRUE,
  nproc = 1,
  verbose = TRUE,
  cmd_is_run = TRUE,
  return_file_path = FALSE,
  start_with_fw = FALSE,
  output_json = FALSE,
  error_tolerance = 0.1,
  args_before_cutadapt = paste0("source ~/miniforge3/etc/profile.d/conda.sh ",
    "&& conda activate cutadaptenv && ")
)
}
\arguments{
\item{ref_fasta}{(Character, required) Path to a FASTA file (plain or
gzip).}

\item{output}{(Character) Path to the output FASTA file. If NULL, defaults
to \verb{\{basename\}_cutadapted.fasta}.}

\item{primer_fw}{(Character, required) The forward primer DNA sequence.}

\item{primer_rev}{(Character, required) The reverse primer DNA sequence.}

\item{discard_untrimmed}{(Logical, default \code{TRUE}) Discard sequences where
primers were not found.}

\item{nproc}{(Integer, default \code{1}) Number of CPU cores for cutadapt.}

\item{verbose}{(Logical, default \code{TRUE}) Print summary statistics.}

\item{cmd_is_run}{(Logical, default \code{TRUE}) If FALSE, return the command
string without executing it.}

\item{return_file_path}{(Logical, default \code{FALSE}) If TRUE, return the
output file path instead of the command.}

\item{start_with_fw}{(Logical, default \code{FALSE}) If TRUE, the forward
primer must be anchored at the start of the sequence.}

\item{output_json}{(Logical, default \code{FALSE}) If TRUE, write a JSON
summary of the cutadapt process.}

\item{error_tolerance}{(Numeric, default \code{0.1}) Maximum error rate for
primer matching.}

\item{args_before_cutadapt}{(Character) Shell commands to run before
cutadapt (e.g., conda activation).}
}
\value{
The cutadapt command string, or the output file path if
\code{return_file_path = TRUE}.
}
\description{
Removes pairs of primers and flanking regions from a FASTA reference
database using \href{https://github.com/marcelm/cutadapt/}{cutadapt}.
Uses linked adapters to trim between forward and reverse primers.
}
\details{
This function is mainly a wrapper of the work of others.
Please cite cutadapt (\doi{doi:10.14806/ej.17.1.200}).
}
\examples{
\dontshow{if (tolower(Sys.info()[["sysname"]]) != "windows") withAutoprint(\{ # examplesIf}
\dontrun{
cutadapt_rm_primers_db(
  "database.fasta.gz",
  output = "db_cutadapted.fasta",
  primer_fw = "GCATCGATGAAGAACGCAGC",
  primer_rev = "TCCTCCGCTTATTGATATGC"
)
}
\dontshow{\}) # examplesIf}
}
\author{
Adrien Taudière
}
