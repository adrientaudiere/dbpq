# Build one Venn or UpSet plot for a rank's membership table

Build one Venn or UpSet plot for a rank's membership table

## Usage

``` r
comparison_plot_one(
  membership,
  db_labels,
  rank,
  weight_by_seqs = FALSE,
  venn_max = 7L
)
```
