# Read lines from a plain or gzipped file

Read lines from a plain or gzipped file

## Usage

``` r
read_lines_db(file_path)
```

## Arguments

- file_path:

  (Character, required) Path to a file (plain or gzip).

## Value

A character vector of lines.
