# Extract a long taxon table (file, rank, taxon, n_seqs) from a FASTA file

Extract a long taxon table (file, rank, taxon, n_seqs) from a FASTA file

## Usage

``` r
extract_taxa_long_db(file, tax_format = "auto")
```
