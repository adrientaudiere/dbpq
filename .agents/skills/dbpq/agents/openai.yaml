interface:
  display_name: "Dbpq"
  short_description: "Repo-specific patterns and workflows for dbpq"
  default_prompt: "Use the dbpq repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true